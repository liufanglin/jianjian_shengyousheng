<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充 -->
    <solid android:color="@color/white" />
    <!-- 圆角 -->
    <corners android:topLeftRadius="10dp"
            android:bottomRightRadius="5dp" />

    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/mianColor" />

</shape>